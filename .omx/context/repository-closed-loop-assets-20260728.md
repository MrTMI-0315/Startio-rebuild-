# Startio 저장소 기반 개발 루프 — 컨텍스트 스냅샷

- 작성일: 2026-07-28
- 목적: 현재 개발 속도를 해치지 않으면서 계획 → 구현 → 검증 → Git → 설명 → 피드백 루프에 재사용 가능한 자산을 남기는 최소 운영안을 결정한다.
- 비교 기준: `0855a35` (`main`, `origin/main`)

## 현재 사실

- 제품 기준 문서는 `docs/canonical/`과 `DESIGN.md`에 이미 존재한다.
- 구현 방향과 MB-01~20 이력은 `.omx/plans/micro-build-startio-rebuild.md`에 존재한다.
- 제품 PRD와 검증 계약은 `.omx/plans/prd-startio-mobile-v1-reset.md`, `.omx/plans/test-spec-startio-mobile-v1-reset.md`에 존재한다.
- 두 번째 실기기 Smoke의 P0~P2 과제와 상태는 `.omx/plans/smoke-2-priority-checklist.md`에 존재한다.
- Lore 형식의 첫 기준 커밋 `0855a35`가 원격 `main`에 push되어 있다.
- 최근 변경 설명은 `$explain-diff` HTML 산출물로 생성했으나 `.omx/artifacts/`는 저장소에 커밋하지 않는다.
- 현재 제품 코드는 MB-12~20까지 구현·자동 검증되었고, 다음 제품 작업은 실기기 P0 판정과 P1 시각 밀도 교정이다.

## 문제

제안된 `work-brief.md`, `plan.md`, `test-spec.md`, `handoff.md`, `questions.md`를 매 작업마다 별도 생성하면 현재 자산과 역할이 겹친다. 특히 한 명이 제품·디자인·개발·실기기 QA를 함께 수행하는 현재 단계에서는 문서 동기화가 구현보다 커질 수 있다.

## 결정이 필요한 질문

1. 항상 유지해야 하는 최소 상태 파일은 몇 개인가?
2. 기존 canonical·micro-build·test spec·smoke checklist 중 무엇을 재사용하고 무엇을 새로 만들지 않는가?
3. `$ralplan`, `$explain-diff`, commit/push를 어떤 작업 크기에서만 실행할 것인가?
4. 운영 비용이 개발 속도를 침해했다고 판단할 정량 기준은 무엇인가?
5. 몇 회의 실제 루프를 거친 뒤 스크립트나 별도 하네스를 고려할 것인가?

## 제약

- 새 dependency와 별도 자동화 하네스는 지금 추가하지 않는다.
- 제품 구현과 검증을 문서 생산 때문에 지연하지 않는다.
- 개인정보·안전·데이터 계약 변경은 항상 정식 계획·검증 게이트를 거친다.
- 상태 원천은 대화 기록이 아니라 저장소 파일과 Git 이력으로 둔다.
- 다음 제품 작업인 P1 시각 밀도 교정을 막지 않는 수준으로 운영안을 제한한다.

## 성공 기준

- 다음 제품 작업에서 운영 문서 작성·갱신 시간이 15분 이내다.
- 같은 결정이나 완료 상태를 두 파일 이상에 수동으로 복제하지 않는다.
- 작업 재개 시 5분 안에 현재 목표, 완료 상태, 다음 검증, 열린 질문을 찾을 수 있다.
- commit 전에 실행한 검증과 검증하지 못한 항목이 남는다.
- 작업자 피드백은 다음 작업의 단일 상태 원천에 반영된다.
