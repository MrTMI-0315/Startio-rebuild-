# Mobile-safe public configuration only.
# Never place OpenAI or store signing secrets in the Expo client.
# The app appends /api/coach/plan and sends a task only after explicit consent.
EXPO_PUBLIC_STARTIO_API_BASE_URL=
